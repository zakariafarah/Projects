from Person import Person
from Course import Course

class Professor:
    def __init__(self, first, last, dob, number, Address, status, salary):
        self.first = first
        self.last = last
        self.dob = dob
        self.number = number
        self.Address = Address
        self.salary = salary
        self.courses = []


def check_raise(self):
    if len(self.courses) > 4:
        salary+= 200000

def add_course(self,course):
    self.courses.append(course)


